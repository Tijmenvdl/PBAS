# PBAS
PBAS Work Ahold Case Group 9

run `pip install -r requirements.txt` in vscode terminal before usage, to make sure the proper packages are installed. Contains a lot more than required, but thats because I froze my full local package setup into the file for my own convenience. If you add another package through your code, update the requirements folder by stating `pip freeze > requirements.txt` in terminal.